%% Allow emojis :_D
\usepackage{fontspec}

%% Seems like setting this on top fixes the language problem
\usepackage[T1]{fontenc}
\usepackage[spanish, es-noshorthands, shorthands=off]{babel}
\usepackage{hyperref}

%% This has to be after setting the language
\directlua{luaotfload.add_fallback
   ("emojifallback",
    {
      "NotoColorEmoji:mode=harf;"
    }
   )}
\setmainfont{Atkinson Hyperlegible}[
  RawFeature={fallback=emojifallback}
]

\setmonofont{Hack}[
  RawFeature={fallback=emojifallback}
]

%% \usepackage[utf8]{inputenc}
%% \usepackage[T1]{fontenc}

%% Control borders of page
%% \usepackage{setspace}
%% \onehalfspacing
%% \usepackage[top=-2cm, bottom=-2cm, left=-2cm, right=-2cm]{geometry}
%% \usepackage{anysize}
%% \marginsize{0 cm}{0 cm}{0 cm}{0 cm}
%% \usepackage[margin=-6 cm]{geometry}
\usepackage[left=0mm, right=0mm, top=0mm, bottom=0mm]{geometry}
%% Allow changing font size
\usepackage{extsizes}

%% add pagebreak after h1
%% \usepackage{titlesec}
%% \newcommand{\sectionbreak}{\clearpage}
